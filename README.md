# Notas:

Mi primer script en Node.

# Comandos:

```
Options:
    --help              Show help                                    [boolean]
    --version           Show version number                          [boolean]
    -b, --base              Crear la tabla de multiplicar según su base
                                                             [number] [required]
    -l, --listar            Mostrar la tabla en la consola
                                                      [boolean] [default: false]
    --l_b, --limitBase  Cantidad de filas a calcular    [number] [default: 10]
```